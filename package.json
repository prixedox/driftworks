{
  "name": "driftworks",
  "private": true,
  "version": "0.0.1",
  "type": "module",
  "description": "DRIFTWORKS — a 2.5D automation prototype. Build a factory that walks.",
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview --host"
  },
  "dependencies": {
    "three": "^0.169.0"
  },
  "devDependencies": {
    "@types/three": "^0.169.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
